# 📘 Today I Learned

### 1. 오늘 배운 내용
- 표준 입출력 (Scanner 클래스): 자바의 기본 라이브러리인 java.util.Scanner를 호출하여 콘솔창으로 사용자 데이터를 입력받는 흐름을 익혔습니다. nextInt()와 next()의 차이를 이해하고 적절한 상황에 사용했습니다. 
- 기본형 vs 참조형 데이터 타입: 아기사자의 인원수를 저장할 때는 정수형(int)을, 이름을 저장할 때는 문자열(String) 타입을 사용하며 각 데이터 타입의 특성을 복습했습니다. 
- 제어문을 통한 프로그램 흐름 제어: - while(true) 무한 루프를 활용해 조건이 충족될 때까지 입력을 반복시키는 구조를 구현했습니다. 
- if-else 조건문으로 입력값이 5 미만인 경우를 판별하여 사용자에게 오류 메시지를 출력하는 '유효성 검사' 로직을 작성했습니다. 
- 배열(Array)의 선언과 활용: new String[count]와 같이 런타임에 입력받은 변수를 활용해 배열의 크기를 동적으로 지정하는 법을 배웠습니다. 
- 반복문(for)을 이용한 배열 순회: 배열의 인덱스(i)를 활용해 0번 인덱스부터 마지막 인덱스까지 순차적으로 접근하여 데이터를 저장하고, 다시 출력하는 전체적인 프로그램의 '입력-처리-출력' 흐름을 완성했습니다.

### 2. 핵심 정리 (내 언어로)
- 자바에서 배열은 한 번 크기를 정하면 바꿀 수 없다는 특징이 있습니다. 그래서 이번 과제처럼 사용자에게 먼저 숫자를 입력받고, 그 숫자를 변수로 활용해 배열의 크기를 선언하는 과정이 논리적으로 어떤 순서로 이루어져야 하는지 확실히 정리했습니다.
- 또한 배열의 첫 번째 요소는 0번부터 시작한다는 점을 유념하며, for문의 조건식을 배열의 길이만큼 설정해 데이터를 순서대로 넣고 빼는 과정이 프로그래밍의 가장 기본적인 데이터 처리 흐름이라는 것을 익혔습니다.
- 그리고 main 메서드 안에서 입출력, 조건, 반복이 순차적으로 일어나는 절차적 흐름을 탄탄하게 익히는 것이 중요하다고 생각하게 되었습니다.

### 3. 결과 이미지(스크린샷)
-

### 4. 느낀 점
- 자바의 기초 문법을 실제 프로젝트로 구현해 보면서, 단순한 이론 공부보다 직접 코드를 짜보는 실습이 개념을 익히기에 더욱 효과적이라는 것을 느낄 수 있었습니다.
- 특히 스스로 고민하며, 프로그램의 논리적 흐름을 설계하는 재미를 느꼈습니다.
- 처음에는 인텔리제이 사용법이나 GitHub 연동 과정에서 예상치 못한 오류를 마주해 당황하기도 했지만, 차근차근 문제를 해결해 나가며 개발 환경에 조금은 익숙해질 수 있었습니다.
- 앞으로 이어지는 과제도 완벽히 해낼 수 있도록 더욱 노력해야겠다는 생각을 하게 되었습니다.